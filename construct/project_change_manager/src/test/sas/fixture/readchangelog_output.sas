DATA WORK.changeLogResult;
	ATTRIB file				length=$1000;
	ATTRIB logicalFilePath	length=$1000;
	ATTRIB noAutoexec		length=$1;
	ATTRIB pcmVersion		length=$20;
	ATTRIB schemaVersion	length=$20;
	
	set _NULL_;
RUN;